version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      - MONGO_URL=mongodb+srv://grupo2:tpa2025@birbnb.j2iy45j.mongodb.net/birbnb?retryWrites=true&w=majority
    depends_on:
      - frontend

  frontend:
    build: ./frontend
    ports:
      - "4000:4000"
    volumes:
      - ./frontend:/app
      - /app/node_modules